# Slice 1.2.8 A Follow-up

Desktop allows an administrator to send an unpaid, daily-unclosed salida to `en espera` for review.

API and Mobile do not expose this action in this slice. Before adding parity, they must use the same daily-close, print-ticket, and `reversiones_salida` audit safeguards as Desktop.
